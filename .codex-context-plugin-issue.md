# Claude Code Plugin Issue Analysis

## 問題
`~/.claude/plugins/claude-code-harness` のプラグインコマンド（`/harness-init` など）が実行できない。

## 現状の構成

### 1. プラグインディレクトリ構造
```
~/.claude/plugins/claude-code-harness/
├── .claude-plugin/
│   ├── hooks.json      # フック定義
│   ├── marketplace.json
│   └── plugin.json     # プラグインメタデータ (v2.7.15)
├── commands/
│   ├── core/
│   │   └── harness-init.md  # ← これが /harness-init コマンド
│   ├── handoff/
│   └── optional/
├── skills/             # サブディレクトリごとにスキル定義
├── scripts/            # Node.jsスクリプト
└── ...
```

### 2. settings.json の設定
```json
{
  "enabledPlugins": {
    "claude-code-harness@Chachamaru127-claude-code-harness": true
  }
}
```

### 3. plugin.json の内容
```json
{
  "name": "claude-code-harness",
  "version": "2.7.15",
  "description": "Claude harness - A harness for solo developers..."
}
```

### 4. hooks.json は存在し、SessionStart等のフックが定義されている

## 疑問点
1. commands/ ディレクトリ内の .md ファイルはどのように登録されるのか？
2. skills/ ディレクトリ内のスキルはどのように登録されるのか？
3. Claude Codeが `/harness-init` を認識するために必要な設定は何か？
4. プラグインの manifest.json は不要なのか？（plugin.json と hooks.json で十分？）

## 期待する回答
- プラグインコマンドが認識されない原因
- 必要な修正または設定
- Claude Code プラグインシステムの仕組み
